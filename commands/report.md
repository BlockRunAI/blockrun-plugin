---
description: Show BlockRun spending report (this session + wallet total)
---

Call `blockrun_wallet` with `action: "report"` to show cumulative spending and
any per-agent budgets. Then summarize, as a short markdown table, what I've
spent so far and on which tools this session (the `[BlockRun] … session $…`
receipts in our conversation are the per-call costs).
